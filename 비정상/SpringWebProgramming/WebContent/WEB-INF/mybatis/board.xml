<?xml version="1.0" encoding="UTF-8"?>

<!-- 밑에서 작업을 할때 태그 작성을 할때는 DOCTYPE 을 준수해야 된다. root 태그를 매퍼로 작성해야 된다.
public 은 공개 이름 그다음은dtd 실제 다운로드 하는 경로 공개이름을 인식하고 있는 xml 에디터면 상관이 없다.mybatis 를 식별하는 식별자  루트태그 mapper 이다.
doctype 가 없을 경우는 단순한xml 파일이다.-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <!-- namespace는 파일을 구분하는 것이다. 마이 바티스는 ?안쓰고 #{} 쓴다.보드라는 객체가 잇는데 필드값을 넣어라 는 표시이다,#{} 파라미터 타입은 내가 가지고 있는 객체가 있는데 마이바티스를 통해서 
  테이블에 집어 넣으로고 한다.어떤 객체를 보드라고하는 테이블에 집어 넣을거냐 -->
  <mapper namespace="board">
  	<insert id="insert" parameterType="com.mycompany.myapp.dto.Exam12Board"><!-- exam12board를 찾아서 값을 집어 넣어라 의미이다. -->
  	insert into board 
	(bno, btitle, bcontent, bwriter, bdate, bpassword, bhitcount, boriginalfilename, bsavedfilename, bfilecontent) 
	values 
	(board_bno_seq.nextval, #{btitle}, #{bcontent}, #{bwriter}, sysdate, #{bpassword}, 0, #{boriginalfilename}, #{bsavedfilename}, #{bfilecontent})
  	</insert>
  </mapper>
  

