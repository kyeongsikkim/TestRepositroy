String Tokenizer 클래스
구분자로 자르는것

string split() 메소드 이용 
java.util.string Tokenizer 이용

countToken(); int 형 반환
hasMoreTokens();
nextToken();
String data="ABC";
data+="DEF";

StringBuffer 은 내부에서 조작이 가능하다.
StringBuilder 
멀티 스레드는 StringBuffer
단일 스레드는 StringBuilder

append 는 제일 뒤에 추가하는거
insert 는 문자열 중간에 추가
delete 는 삭제
replace
StringBulder reverse()
setCharAt

정규식 정해져 있는 표현

문자열로 받아들이기 위해서 역슬러시 하나 더 넣어줘야한다.

기본값으로 객체를 갖게 된다면 값을 바꾸지 못한다.
박싱은 기본값을 객체로 만드는것 언박싱은 객체 안에 있는 기본값을 빼내는것 

필드가 대문자이면 상수이다.


