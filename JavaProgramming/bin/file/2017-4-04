java.lang 은 따로import 없이 사용이 가능하다.

java.util util 안에 들어있는 클래스들 을 사용할 수있다.

java.lang.Object
java.lang.String 상속관계이다.

public final class String 파이널은 상속이 안된다. final 클래스는 상속 불가

 static final 상수이다.
/ 
 Deprecated.  이전버전까지는 많이 사용 됐지만 현재는  가급적이면 사용하지마라
 String(byte[] ascii, int hibyte)
Deprecated. 
This method does not properly convert bytes into characters. As of JDK 1.1,
 the preferred way to do this is via the String constructors 
 that take a Charset, charset name, 
or that use the platform's default charset./

static 메소드는 static 붙어있다.객체생성없이 접근 가능하다.
앞에는 리턴값 

java.lang 는 import 없이 사용 가능하다

Object system class String StringBuffer,StringBuilder ,Math,Wrapper 클래스 
어떤 클래스를 상속하지 않아도 자동적으로 extends 를 상속하게 된다.

equals 는 객체 비교 String 클래스는equals 를 재정의 해서 String 객체간의 문자열을 비교한다.

객체의 번지는 다른데 아이디가 같으면 같은 객체로 간주하자

String 도 equals 를 재정의 해서 나타낸 것이다.
obj 에서 equals == 나 equals 나 똑같은 것이다.
그래서 equals 를 재정의를 해서 사용을 한다.

해쉬코드
객체의 해시코드란 객체를 식별할 하나의 정수값을 말한다.
객체가 다른 것은 해쉬코드로 구별한다.
객체가 가지고 있는 해쉬코드는 JVM 이 자동으로 해쉬코드를 자동적으로 생성한다.자동적으로 생성한 것을 얻고 
싶으면 obj 의 hashcode() 를 호출해 봐라 그렇다면 해쉬코드 값이 나온다.

equals 의 타입은 boolean 타입이다.

hashset 은 객체를 비교할 때 두가지 방법을 사용한다. 1.해시 코드를 먼저 비교한다.2 해시코드가 같을 경우 equals() 메소드를 이용해서 
비교를 했을 때 true 가 나오면 완전히 같은 객체로 판단을 한다.
같은 객체가 있을 경우는 기존에 있는 같은 객체를 없애버리고 새로운 객체를 받아들인다. 
해쉬코드를 먼저 검사 두번째 equals 를 검사 완전동등할 경우는 없애고 새것을 받아 들인다.
해시 코드는 무조건 다르다. 객체가 생성이 되면 
해시코드와 equals 를 재정의 해야 된다.
class system{
public static PrintStream out;
public static inputStream in;
private 생성자가 붙어 있는 경우는 안보여준다 객체를 못만든다.
객체를 못만들면 100프로 static 이 붙어있는 것이다.
///////////////////////////////////////////////////////////////////////////
	at ch11.exam05.SystemExample$1.checkExit(SystemExample.java:11)
4
5
	at java.lang.Runtime.exit(Runtime.java:107)
	at java.lang.System.exit(System.java:971)
	at ch11.exam05.SystemExample.main(SystemExample.java:21)
	밑에서부터 분석을 한다. 우리가 작성한 클래스를 먼저 확인 한다.
///////////////////////////////////////////////////////////////
	예외 처리가 발생하면 프로그램이 바로 종료가 된다. 종료가 안되게 하기 위해서는 
	예외처리를 해줘야 된다. try catch 를 잡아줄경우는 바로 종료가 안된다.
	JVM 에게 가능한한 빨리 GC 를 실행해달라고 요청한다.

properties
	키값을 얻어내는것 키값과 value 의 값은 string 형태이다.
	
String 이 가지고 있는 메소드 정도는 많이 알것이다.	
배열은 기본값으로 초기화된다.