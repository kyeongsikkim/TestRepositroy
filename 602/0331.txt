
p.329 추상클래스
Tire - roll()  -왜 안쓰는데 있을까? 이게 없으면 재정의를 할 수 없어. 
	블록없이 있다정도만 표시. 실제 실행할 내용은 없음NormalTire - roll(){}
SnowTire -roll(){}


추상이란? 
추상클래스란? 이런클래스가있다~정도 
	- 객체를 생성하여 사용할 수 없다. new쓸수 없다. 
	-상속으로만 사용해야 한다. 
	-자식클래스에서는 구체화해야한다(실행 내용을 가지고 있어야 한다)
-like미완성설계도
-규격을 정의. 규격을 맞추어서 정의해야해.
만들지않으면 컴파일 되지 않는 방법은?

sound를 만들어서 (규격을 정의)
자식클래스에서 재정의


p.331
자동차 설계할때 타이어이 규격을 선언.
장착하는 실체 객체는 어떻게 만드나? 실제 객체 클래스를 장착

p.332
abstract
절대 단독으로 객체를 생성할수 없다.상속을 통해 써야한다.

추상메소드 - 구체적으로 어떤 기능인지 모르니까 메소드를 선언한다.
	-클래스가 반드시 추상클래스.
	-있다만 표시, 중괄표 표시안함
	
(exam08)
자식클래스에서 재정의를 하지않으면 컴파일 에러나


hw. 7장 예제+연습문제 

ClassCastException 변환이 잘못되었다.
instanceof






p.344 인터페이스 
:접점의 역활.
ex. 티비. 리모콘(티비와 나의 인터페이스)

왜 인터페이스를 쓸까? 인터페이스 사용방법만 알면되니까 -객체마다 코드가 있는데 그거를 인터페이스에 통합
	-객체의 사용방법을 통일화
*사용방법을 통일화-인터페이스
*설계할때 같은 규격으로 - 추상클래스


인터페이스선언
[public] interface 인터페이스명 {...}

메소드의 실행 부분이 없어-추상메소드와 같음
상수
추상메소드  //java7 //객체의사용방법설명.객체의 메소드를 어떻게 호출하는가
			메소드 이름, 매개값,결과값 /실행부있으면안돼
			default가 abstract

defafult타입메소드명
static타입메소드면 //java8 -왜실행부분추가? 람다식을넣을려면.....

사용설며ㅇ서에 데이 터 못넣자나 ... 상수만 넣을수 있음-static final안써도됨..값은반드시줘야함



(exam08)
public static final안붙여.생략!


인터페이스를 구현한 객체.

implements부터 작성->메소드 작성(재정의)





public static void move(Bike bike){
}
public static void move(Car car){
}
-이런식으로 종류마다 다 만들순...없어

public static void move(Manual manual){
		manual.turnOn();
		manual.setSpeed(10);
		manual.run();
		manual.turnOff();
}
-ㅇㅣ렇게 하면 
move(new Bike());
move(new Car()); 
-이렇게 호출할수 있어 
 
move는 Bike와 Car를 모르고 Manual만알고있어



다형성구현방법 
-상속  -공통된 특성을 물려받아 사용
-인터페이스 -설명서로 사용          -더많이 사용.....





===============================================================================






